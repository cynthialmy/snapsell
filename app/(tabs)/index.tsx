// The home screen is at app/index.tsx
// This file exists to make the tab route work
// The actual home screen handles both authenticated and unauthenticated states
// We'll just re-export the home screen component
export { default } from '../index';
